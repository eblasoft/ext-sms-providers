{
    "name": "espocrm-ext-sms-providers",
    "version": "1.6.1",
    "description": "",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build-extension": "node build --extension",
        "build-copy": "node build --copy && cd site && php clear_cache.php"
    },
    "author": "EspoCRM",
    "dependencies": {
        "archiver": "^4.0.1",
        "fs-extra": "^9.0.0",
        "mv": "^2.1.1",
        "request": "^2.88.2",
        "unzipper": "^0.10.11"
    }
}
